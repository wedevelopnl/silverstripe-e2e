---
Name: app-e2e-fixtures
Only:
  environment: dev
---
WeDevelop\E2e\Fixtures\FixtureLoader:
  # Everything this harness app holds is fixture data, so a reset owns the whole
  # page tree. ErrorPage goes with it (it extends Page); dev/build restores it.
  purge_classes:
    - Page
  fixture_page_classes:
    - Page
  fixtures:
    demo-home: 'wedevelopnl/silverstripe-e2e:tests/E2E/Fixture/demo.yml'
